@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="card" style="max-width:480px;margin:48px auto;">
    <div class="empty-state">
        <svg class="empty-state-icon" width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="m15 9-6 6"/><path d="m9 9 6 6"/></svg>
        <h3 class="empty-state-title">Acceso denegado</h3>
        <p class="empty-state-message">No tienes permisos para acceder a esta página.</p>
        <a asp-controller="Home" asp-action="Index" class="btn btn--default">Volver al inicio</a>
    </div>
</div>